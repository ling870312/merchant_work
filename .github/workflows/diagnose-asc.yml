name: Diagnose ASC Build Status

on:
  workflow_dispatch:
    inputs:
      oem_channel:
        description: 'OEM 渠道码（须与 Secret 命名一致）'
        required: true

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Write P8 key
        env:
          ASC_API_KEY_P8: ${{ secrets[format('ASC_API_KEY_P8_{0}', inputs.oem_channel)] }}
          ASC_API_KEY_ID: ${{ secrets[format('ASC_API_KEY_ID_{0}', inputs.oem_channel)] }}
        run: |
          [ -n "$ASC_API_KEY_ID" ] || { echo "::error::未配置渠道 ${{ inputs.oem_channel }} 的 ASC_API_KEY_ID_${{ inputs.oem_channel }} Secret"; exit 1; }
          [ -n "$ASC_API_KEY_P8" ] || { echo "::error::未配置渠道 ${{ inputs.oem_channel }} 的 ASC_API_KEY_P8_${{ inputs.oem_channel }} Secret"; exit 1; }
          mkdir -p "$HOME/private_keys"
          printf '%s' "$ASC_API_KEY_P8" > "$HOME/private_keys/AuthKey_${ASC_API_KEY_ID}.p8"
          chmod 600 "$HOME/private_keys/AuthKey_${ASC_API_KEY_ID}.p8"

      - name: Resolve channel bundleId (from Secret)
        env:
          CHANNEL_BUNDLE_ID: ${{ secrets[format('IOS_BUNDLE_ID_{0}', inputs.oem_channel)] }}
        run: |
          set -eu
          CHANNEL="${{ inputs.oem_channel }}"
          BID="${CHANNEL_BUNDLE_ID:-}"
          [ -n "$BID" ] || { echo "::error::未配置渠道 ${CHANNEL} 的 bundleId Secret (IOS_BUNDLE_ID_${CHANNEL})"; exit 1; }
          printf "::add-mask::%s\n" "$BID"
          echo "BUNDLE_ID=$BID" >> "$GITHUB_ENV"
          echo "bundleId resolved for channel: $CHANNEL"

      - name: Query ASC builds
        env:
          ASC_API_KEY_ID: ${{ secrets[format('ASC_API_KEY_ID_{0}', inputs.oem_channel)] }}
          ASC_ISSUER_ID: ${{ secrets[format('ASC_ISSUER_ID_{0}', inputs.oem_channel)] }}
          BUNDLE_ID: ${{ env.BUNDLE_ID }}
        run: |
          [ -n "$ASC_ISSUER_ID" ] || { echo "::error::未配置渠道 ${{ inputs.oem_channel }} 的 ASC_ISSUER_ID_${{ inputs.oem_channel }} Secret"; exit 1; }
          export P8_FILE="$HOME/private_keys/AuthKey_${ASC_API_KEY_ID}.p8"
          ruby scripts/ci/diagnose-asc.rb
